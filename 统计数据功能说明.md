# 资产列表统计数据功能说明

## 功能概述

资产列表页面的统计卡片区域已经实现了根据本地存储的资产数据实时显示各项统计指标的功能。统计项包括：

- 总资产数
- 在用资产数及百分比
- 闲置资产数及百分比
- 维修中资产数及百分比
- 损坏资产数及百分比

## 技术实现

统计功能主要通过以下方式实现：

1. **数据来源**：统计数据来源于存储在浏览器本地存储(LocalStorage)中的资产数据
2. **统计计算**：在`updateStatistics()`函数中实现了资产数据的统计计算
3. **实时更新**：在资产数据发生变化时(添加、修改、删除、导入等操作)，系统会自动调用`updateStatistics()`函数更新统计数据
4. **页面加载**：页面加载完成后，系统会自动从本地存储加载数据并更新统计信息

## 验证方法

为了帮助您验证统计功能的正常工作，我们提供了一个测试数据脚本。请按照以下步骤操作：

1. 打开资产列表页面
2. 按`F12`键打开浏览器的开发者工具
3. 切换到"控制台"(Console)选项卡
4. 在控制台中输入以下命令并按回车键：
   ```javascript
   loadTestData()
   ```
5. 系统会提示是否替换现有数据，点击"确定"
6. 稍等片刻，您将看到统计卡片上的数据自动更新，显示测试数据的统计结果

## 测试数据说明

测试数据脚本(`test-data.js`)包含了6条不同状态的模拟资产数据：

- 3台在用设备
- 1台闲置设备
- 1台维修中设备
- 1台损坏设备

执行测试数据加载后，您可以观察到统计卡片上的数据会相应地更新，显示各项统计指标的数量和百分比。

## 注意事项

1. 执行`loadTestData()`函数会替换当前所有的资产数据，请确保在测试前已备份重要数据
2. 统计数据会在资产数据发生任何变化时自动更新，无需手动刷新
3. 如果统计数据未能正确显示，请刷新页面后重试

## 常见问题

**Q: 为什么统计数据显示为0？**
A: 可能是因为本地存储中没有资产数据。您可以通过导入数据或执行测试数据脚本来添加资产数据。

**Q: 为什么维修中资产的统计数据没有显示？**
A: 已修复此问题，现在统计功能可以正确显示所有状态的资产数量。